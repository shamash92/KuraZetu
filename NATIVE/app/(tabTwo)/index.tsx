import {Text, View} from "react-native";

import React from "react";

const TabTwoComponent = () => {
    return (
        <View style={{flex: 1, justifyContent: "center", alignItems: "center"}}>
            <Text>TabTwoComponent</Text>
        </View>
    );
};

export default TabTwoComponent;
